name: baseline_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scikit-learn
  - xgboost
  - tqdm
  - pip:
    - torch
    - tensorflow
    - keras
